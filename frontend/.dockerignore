# Dependency directories
node_modules
.next
.git

# Temporary files and logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local

# Configuration files that are not needed in the final image
# (though they are small and usually harmless)
.env.local