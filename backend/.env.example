# ℹ Rename file to .env and edit values for production
# # can generate a strong secret key using:```import secrets, secrets.token_urlsafe(32)```

# Application
PROJECT_NAME="LMS MVP Backend"
API_V1_PREFIX="/api/v1"

# DB — SQLite for local dev, switch to Postgres for cloud
#DATABASE_URL=sqlite:///./app.db
DATABASE_URL=postgresql+psycopg2://postgres:postgres@postgres:5432/lmsdb

# JWT
SECRET_KEY="change_this_to_a_long_random_secure_key"
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS
CORS_ORIGINS=["http://localhost:3000", "https://your-frontend-domain.com"]

# Debug user creation toggle (dev-only)
FIRST_SUPERUSER_EMAIL="admin@example.com"
FIRST_SUPERUSER_PASSWORD="Admin123!"
